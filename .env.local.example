NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only: used by the Python detector script to write into devices/alerts,
# bypassing RLS. Never expose this to the browser.
SUPABASE_SERVICE_ROLE_KEY=
